# 目前的工作分为两部分，一部分是解决当前存在的问题；另一部分是根据给出的“功能文档”文件，进行每个功能的完善和测试。

代码已经可以编译但仍存在以下问题

## 当前存在的问题

### 最近前台应用异常

#### （1）“传感器数据”页面中的“最近前台应用”模块在我的小米手机上（Android 15）无法显示任何应用包名，而在 Android Studio 的虚拟机上能够显示，但不正常；

#### （2）“最近前台应用”模块在 Android Studio 的虚拟机上目前存在两个问题：1是无法实时显示（比如切换app后切回来）；2是包名会有重复，想要实现的效果是按时间顺序排序，最后使用的显示在最上段。

### UI修改

#### （1）将“隐私设置”页面中的“数据管理”“传输策略”“加密状态”放置在页面的“文件队列下方”，删除“数据保留期限”（无该功能）。

#### （2）将“详细信息”页面中的“传输状态”“gRPC状态”“缓冲区统计”“NTP时间同步”转移至“服务器配置”页面的“文件队列下方”

#### （3）将“隐私设置”页面改为“持续认证”，暂定为空，用来与服务端交互获取认证结果等信息。




## 功能完善及测试

### 功能文档中的大部分功能基本完成，后续工作包括

#### （1）仔细对照功能文档，完善每一个功能模块；

#### （2）仔细对照功能文档，对每一个功能模块进行测试；

#### （3）仔细对照功能文档，预留与服务端交互的相关接口；

#### （4）仔细对照功能文档，完善每一个功能模块；